FROM python:3.10-slim

WORKDIR /app

# FIX: This line now correctly copies the specific requirements file
COPY requirements.mavsdk.txt requirements.txt
COPY telemetry_mqtt.py .

RUN pip install --upgrade pip && pip install -r requirements.txt

ENTRYPOINT ["python3", "telemetry_mqtt.py"]